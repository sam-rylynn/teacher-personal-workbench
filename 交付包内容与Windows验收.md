# 教师个人工作台 · 交付包与 Windows/macOS 验收 Runbook

> 文档口径：`v0.5.0` 候选版 ｜ 更新日期：2026-08-09。

## 一、当前验证状态

| 项目 | 当前状态 |
|---|---|
| 版本 | `package.json` 为 `0.5.0` 候选口径 |
| 正式发布 | 未由本文档确认；交付时必须记录实际提交或标签 |
| Windows 实机 | 尚未在本文档中记录通过结果 |
| macOS 交付实机 | 尚未在本文档中记录通过结果 |
| CI | 仓库可包含工作流配置，但本文档没有对应通过记录 |

启动脚本存在、代码能构建或 CI 配置存在，都不能替代目标设备实测。完成下方 Runbook 后，才可以在交付验收单中勾选相应结果。

## 二、交付包应包含

| 项目 | 内容 |
|---|---|
| 源码 | 与交付提交一致的完整私有仓库内容 |
| 依赖锁定 | `package.json`、`package-lock.json`、`.node-version`、`.nvmrc` |
| 通用启动 | `scripts/run-vinext.mjs`、`scripts/launch-workbench.mjs` |
| Windows 启动 | `启动工作台.bat` |
| macOS 启动 | `启动工作台.command` |
| 使用说明 | `README.md`、`销售部署说明.md`、本 Runbook |
| 授权文本 | `授权协议.md` |

交付时另行记录：

- 私有仓库地址；
- 实际提交哈希或正式标签；
- 目标操作系统与浏览器版本；
- 电脑访问地址；
- 手机实际可访问的查看页地址；
- 验收日期和验收人。

以下内容不得进入源码交付包或 Git 历史：真实学生资料、完整 JSON 备份、手机查看文件、手机速记内容、AI/API 密钥和 Git 写入凭据。`dist/` 是执行构建后产生的本机文件，除非交付清单单独记录，否则不默认视为已经随仓库交付。

## 三、安装前提

- Windows 10/11 或当前受支持的 macOS；
- Node.js `22.13.0` 或更高；
- npm 可用；
- Git 可访问老师自己的私有仓库；
- Chrome、Edge 或 Safari 等现代浏览器；
- 如果验收手机查看，需要一个手机能实际访问的工作台页面入口和一台真实手机。

在终端验证版本：

```text
node -v
npm -v
git --version
```

电脑上的 `http://localhost:3002/` 只指向当前电脑，不能当作另一台手机的访问地址。

## 四、Windows 安装与启动

### 1. 获取候选源码

```powershell
git clone <老师的私有仓库地址>
cd teacher-personal-workbench
git checkout <交付提交或标签>
git status --short
```

`git status --short` 应无输出。不要直接使用本文档中的版本号代替实际交付提交。

### 2. 安装依赖

```powershell
npm ci
```

### 3. 在目标提交执行检查

```powershell
npm run verify
```

通过标准是命令退出码为 0。本文档不预填测试数量，也不预先声称 Windows 或 CI 已通过；保存实际终端记录。

### 4. 启动工作台

可以双击 `启动工作台.bat`，也可以在 PowerShell 执行：

```powershell
npm run launch
```

首次启动若没有 `dist/`，脚本会先执行构建。终端显示启动地址后，手动在浏览器打开 `http://localhost:3002/`，并保持终端窗口运行。停止时在终端按 `Ctrl+C`。

如果 3002 端口被占用，可在 PowerShell 改用其他端口：

```powershell
$env:TEACHER_WORKBENCH_PORT="3003"
npm run launch
```

然后打开 `http://localhost:3003/`。

## 五、macOS 安装与启动

### 1. 获取源码并安装

```bash
git clone <老师的私有仓库地址>
cd teacher-personal-workbench
git checkout <交付提交或标签>
git status --short
npm ci
```

### 2. 在目标提交执行检查

```bash
npm run verify
```

### 3. 启动工作台

可以双击 `启动工作台.command`，也可以执行：

```bash
npm run launch
```

终端显示启动地址后，手动打开 `http://localhost:3002/`。如需更换端口：

```bash
TEACHER_WORKBENCH_PORT=3003 npm run launch
```

首次双击 `.command` 如果被 macOS 安全策略阻止，应由安装人员核对文件来源后，在系统允许的方式下打开；不要通过关闭系统安全功能处理。

## 六、桌面端实机验收

在目标电脑和交付约定浏览器中逐项执行：

- [ ] 页面实际加载，桌面宽度下无整页横向滚动；
- [ ] 今日、学生、课表、事项、资料五个入口均可打开；
- [ ] 新增或修改一条测试事项，刷新页面后仍保留；
- [ ] 从表格复制一条测试成绩，导入后显示为待核对；
- [ ] 待核对成绩可编辑、核对并计入，也可删除；
- [ ] 导入一条测试课次，确认周课表显示正确；
- [ ] 新建一条测试重复课次，验证调课时间须成对填写、合法调课可保存并可撤销；
- [ ] 验证错误单次课可删除，删除重复课次会同时移除它的临时调整；
- [ ] 导出 `.ics`，由目标系统日历实际打开检查；
- [ ] 导出完整 JSON 数据文件；
- [ ] 从该文件恢复前显示学生、课次和事项数量确认；
- [ ] 做一次修改后可在本浏览器看到自动备份；
- [ ] 已向老师说明自动备份最多 3 份，清除浏览器站点数据时也会丢失；
- [ ] 浏览器控制台没有影响使用的 error。

验收数据应使用虚构测试内容。不要为了验收把真实学生资料发给安装人员或外部 Agent。

## 七、真实手机查看验收

手机查看不是实时同步，也不是把桌面正式数据部署到手机。必须使用一台真实手机完成以下闭环：

1. 在电脑点击“生成手机查看文件”；
2. 确认文件下载，并提示文件包含学生姓名与摘要；
3. 由老师通过自己控制的方式把文件传到手机；
4. 在手机打开交付时记录的可访问页面，并进入 `?mode=mobile`；
5. 选择手机查看文件并手动导入；
6. 核对今日、学生、课表和事项摘要；
7. 确认手机没有修改学生、成绩、课表和电脑正式事项的入口；
8. 电脑修改正式数据后，确认手机不会自动变化；重新生成、传输和导入新文件后才更新。

验收记录：

- [ ] 实际手机型号与系统版本已记录；
- [ ] 实际手机页面地址已记录；
- [ ] 文件由电脑传到手机并导入成功；
- [ ] 核心内容保持只读；
- [ ] 页面无整页横向滚动；
- [ ] 旧文件提示和无效文件拒绝行为已检查。

手机查看文件只是最小摘要，不是桌面完整 JSON 备份，不可用于恢复电脑工作区。

## 八、手机速记验收

手机速记与桌面正式数据、手机只读摘要分别存储：

- [ ] 在手机新增一条虚构速记；
- [ ] 重新打开手机查看页后，速记仍在当前手机浏览器中；
- [ ] 导入新的手机查看文件，不会把速记写入桌面工作区；
- [ ] 点击“复制速记”，把内容带回电脑；
- [ ] 在电脑“数据与备份”中粘贴并导入为待办事项；
- [ ] 重复导入同一批内容不会重复新增；
- [ ] 已告知老师：清除手机浏览器数据会丢失尚未复制的速记。

手机速记是手机端唯一的独立记录区，不代表手机可以编辑学生、成绩、课表或电脑正式事项。

## 九、Agent、备份与提醒边界验收

- [ ] 页面中没有内置 AI 对话、模型账号或 API 密钥配置；
- [ ] 已说明外部 Agent 只协助部署、排障、代码微调或老师主动选择的文件处理；
- [ ] 已说明把资料交给外部 Agent 或 AI 可能使资料离开本机；
- [ ] 已说明自动备份只在当前电脑浏览器内，最多 3 份；
- [ ] 已让老师另存一份完整 JSON 到自己控制的位置；
- [ ] 已说明手机文件和手机速记均不属于电脑完整备份；
- [ ] 页面打开时检查一条本地提醒；
- [ ] 通过 `.ics` 文件检查系统日历提醒，不宣称存在短信、微信或云推送。

## 十、验收结论

分别记录，不得用一项代替另一项：

| 验收层 | 结果 | 证据位置 |
|---|---|---|
| 目标提交 `npm run verify` | ☐ 通过 ☐ 不通过 | |
| Windows 实机启动与桌面流程 | ☐ 通过 ☐ 不通过 ☐ 未测 | |
| macOS 实机启动与桌面流程 | ☐ 通过 ☐ 不通过 ☐ 未测 | |
| 真实手机文件导入与只读查看 | ☐ 通过 ☐ 不通过 ☐ 未测 | |
| 手机速记回传电脑 | ☐ 通过 ☐ 不通过 ☐ 未测 | |

只要目标客户需要的操作系统或真实手机项仍为“未测”，就不能写“交付验收全部通过”。CI 通过也不能替代 Windows、macOS 或真实手机验收。

## 十一、授权边界摘要

永久个人使用与修改；**禁止转售、传播、代售**。法律条款以仓库内《授权协议.md》为准。
