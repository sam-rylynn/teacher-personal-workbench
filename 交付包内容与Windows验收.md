# 教师个人工作台 · 最终交付包内容与 Windows 验收 Runbook

> 版本：v0.2.0 ｜ 生成日期：2026-08-05 ｜ 用途：把本包内容交给一台 Windows 机器上的另一个 Agent，按 Runbook 跑一次完整验收。

---

## 一、交付包清单

| 项 | 内容 | 位置 |
|---|---|---|
| 源码 | React + Vinext 本地优先工作台 | 私有仓库 `main` 分支 |
| 私有仓库 | `sam-rylynn/teacher-personal-workbench`（PRIVATE） | https://github.com/sam-rylynn/teacher-personal-workbench |
| 版本标签 | `v0.1.0`（P1 基线）、`v0.2.0`（销售就绪） | 仓库 tags |
| 授权协议 | 永久个人使用 + 修改、禁止转售传播、一次性 ¥200 | 仓库根 `授权协议.md` |
| 销售部署说明 | 200 元含/不含、部署流程、交付清单、验收单 | 仓库根 `销售部署说明.md` |
| 首装向导 | 图文 5 步（自动弹出 + 侧栏手动入口） | `app/TeacherWorkbench.tsx` |
| 使用教程 | 见本文档「四、使用教程要点」 | 本文档 |
| 本文件 | 交付清单 + Windows 验收 Runbook | 仓库根 `交付包内容与Windows验收.md` |

## 二、源码校验值

| 项 | 值 |
|---|---|
| 默认分支 | `main` |
| 最新提交（文案优化） | `d19045c` |
| 销售就绪标签 | `v0.2.0` |
| Node 要求 | `>= 22.13.0` |

> 说明：仓库为单仓库本地优先应用，无后端、无数据库，无 `.env` 密钥。`db/schema.ts` 与 `.openai/hosting.json` 为脚手架，未启用云能力。

## 三、Windows 验收 Runbook（交给 Windows Agent 执行）

> 目标：在 Windows 上从零跑通安装、构建、测试、双端浏览器验收。预计 10 分钟。

### 0. 前置条件
- Windows 10/11。
- 安装 Node.js **22.13.0 或更高**（https://nodejs.org，LTS 即可）。验证：
  ```powershell
  node -v   # 应 >= v22.13.0
  npm -v
  ```
- 安装 Git，并配置可访问该私有仓库的 GitHub 凭据（仓库已授权给交付对象）。

### 1. 拉取源码
```powershell
git clone https://github.com/sam-rylynn/teacher-personal-workbench.git
cd teacher-personal-workbench
git checkout v0.2.0
```

### 2. 安装依赖
```powershell
npm install
```

### 3. 代码规范检查
```powershell
npm run lint
```
**通过标准**：无 error 输出（exit 0）。

### 4. 构建 + 自动化测试
```powershell
npm test
```
**通过标准**：末尾输出 `# pass 16`、`# fail 0`。

### 5. 启动开发服务
```powershell
npm run dev -- --port 3002
```
> 跨平台启动器已就绪（`scripts/run-vinext.mjs`），Windows 原生命令行可直接运行。
> **注意**：指定的 3002 端口可能被占用，以启动日志实际打印的 `Local:` 地址为准；用 `netstat -ano | findstr :3002` 或 HTTP 请求确认实际监听端口。

### 6. 桌面端浏览器验收（1440px）
用浏览器打开 `http://localhost:3002/`（或实际端口），确认：
- [ ] 页面加载，无横向滚动条（桌面 1440×900）。
- [ ] 首次运行自动弹出**图文首装向导**（房子插图 + 4 张功能卡）。
- [ ] 侧栏五页导航（今日/学生/课表/事项/资料）均可点击打开。
- [ ] 侧栏显示**个人化品牌名**（如"林老师的工作台"）与头像标记。
- [ ] 学生页有"导入名单与测评"，课表页有"导入课表 / 导出日历(.ics)"。
- [ ] 侧栏有"数据与备份"入口，可打开导出/恢复/备份/清除演示数据面板。
- [ ] 完成一件事（点事项前的圆圈）后，刷新仍在（数据已保存）。
- [ ] 浏览器控制台（F12）无 error。

### 7. 手机端浏览器验收（390px）
打开 `http://localhost:3002/?mode=mobile`，把浏览器窗口收窄或开设备模拟到 390×844，确认：
- [ ] 顶部显示"手机查看版 · 更新于 …"与"仅供查看"标记。
- [ ] 仅今日/学生/课表/事项四个查看导航 + 搜索，**无**新增/编辑/完成/发布按钮。
- [ ] 无横向滚动条。
- [ ] 控制台无 error。

### 8. 收尾
```powershell
# 停止服务（在运行 dev 的终端按 Ctrl+C）
```

### 验收结论
- 若第 3、4 步通过且第 6、7 步全部勾选 → **验收通过**。
- 任一项不通过 → 记录现象、控制台报错截图，回报授权方。

---

## 四、使用教程要点（交付给老师）

1. **桌面端 = 生产端**：学生测评、课表、事项、资料都在电脑上维护，保存即存到这台电脑。
2. **手机端 = 只读**：点"更新手机看板"后，手机打开 `?mode=mobile` 查看最新摘要，不能改。
3. **导入**：学生页/课表页点导入，从 Excel 复制表格直接粘贴，先核对再保存；错的行不会入库。
4. **备份**：数据在这台电脑上，"数据与备份"里可导出 JSON 留底、从文件恢复；每次保存自动留上一版（最多 3 份）。
5. **提醒**：页面开着时到点弹提醒；也可"导出日历(.ics)"导入系统日历。
6. **首装向导**：第一次打开自动引导配置姓名/学段/学科/头像，并清空演示数据。

## 五、个人 Agent 接入部署（交付时协助）

1. 老师准备自己的 AI 账号/密钥（OpenAI、豆包、通义等任一）。
2. 密钥只保存在老师本机，**不写入源码、不进 Git 历史、不进手机内容**。
3. AI 输出是可编辑、可追溯、需老师确认的建议/草稿；不自动给学生或家庭贴固定标签。
4. 对外发送前显示实际资料范围，遵循最小必要。

## 六、授权边界（摘要）

永久个人使用与修改；**禁止转售、传播、代售**。详见仓库内《授权协议.md》。
