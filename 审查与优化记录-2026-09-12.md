# 教师个人工作台：全面审查与优化

范围：在现有学生、课表、事项、资料与手机查看板块内修复问题，不新增业务模块。保留上一轮未提交的老师端文案修改。

## 已修复

### 数据与导入

- 本机数据无法读取时暂停普通保存，并常驻提示恢复入口，避免演示内容覆盖原有数据。明确恢复前保留原值备份；异常原文另存为不自动轮转删除的副本，并提供原文下载。无论原值是否合法，显式恢复前备份失败或读回不一致都会停止覆盖。
- 旧版备份中存在无法转换的记录时整体停止恢复，不再报告成功却丢失个别成绩或课程。
- 表格缺列、重复表头、错列、错误数字和引号不完整都会提示；不再误取相邻列或把非法成绩配置当作默认值。
- 同名跨学科测评正常导入；重复确认不重复写入。新导入成绩仍须老师确认后参与比较。
- 演示文案仅在加载时迁移一次，之后不再自动改写老师输入。

### 学生档案与呈现

- 修复班级筛选后其他班级选项消失；筛选无结果时不再展示和编辑不匹配的学生。
- 不同参考人数不计算名次进退；小数分差舍入，避免长串小数。
- 成绩柱高按真实得分率，低分和零分不再被抬高；每次测评各自标注班级均分位置，不用最新均分代替整段历史。
- 图表最多显示最近八次，完整记录保留在表格中。新录入成绩不再默认填入上次成绩、排名和均分。
- 统一小字最小 12px，保留数字统一字形；侧栏文字操作分行，补充按钮选中状态及弹窗焦点循环、关闭后焦点返回。
- 延续老师端自然表达，替换“核验、复核、事实证据”等不合适的页面用语；内部数据标识仍保留。

### 课表与提醒

- 首页日期与下一节课程使用同一参考时间。演示日期显式标注；真实数据按设备时钟每分钟及页面重新聚焦时刷新。
- 周课表支持上一周、下一周、返回本周，课程卡显示状态；远期固定课也能打开详情。
- 导入时检查实际课程、固定课和已调课时间的重叠；接受 9:00 这样的常见时间格式。
- 修复系统日历事项提醒提前一个预计时长的问题，按老师选择的实际提醒时刻导出。

### 手机查看与速记

- 已有手机内容时，导入失败同样显示明确提示且保留旧内容。
- 查询中点击导航会清除搜索并进入对应页。
- 手机速记校验旧数据，保存失败保留输入，清空前需确认，不静默丢弃坏记录。
- 手机文件按实际时刻比较新旧、按老师时区选日期；真实生成时间与演示课表参考时间分开。
- 长事项保持全文传输；导入导出统一检查整个文件 512KB 上限。

### 启动与依赖

- 启动器检测实际端口占用；默认绑定 localhost。
- 源码新增、修改或删除后自动重新构建，避免 Agent 改完仍显示旧页面。
- Next 及相关依赖升级到安全补丁，锁文件可重新安装；交付包加入构建检查脚本。

## 验证证据

- `npm run verify`：类型检查、Lint、生产构建、43/43 测试通过（包含最后补充的恢复备份失败保护）。
- `npm ci --ignore-scripts --no-audit --no-fund`：锁文件重装通过。
- `npm audit --omit=dev`：0 条告警。
- 真实 `npm run launch` 启动后 localhost 与 127.0.0.1 的页面均返回 HTTP 200；重复启动正确提示端口占用。
- 浏览器实际验证：成绩导入→待确认→确认→刷新保留；班级选项不丢失；无结果不显示旧学生；课表翻周和回本周；首装弹窗 Shift+Tab 循环。
- 手机实际页面验证：查看文件生成；只读页面无桌面编辑按钮；查询后导航正常；速记刷新后保留；错误文件导入有提示且不覆盖旧内容；390px 页面无横向溢出。
- 桌面实际检查 1440px/1280px，无横向溢出；可见小字不低于 12px。浏览器控制台本次手机检查无错误或警告。
- 独立临时副本交付包：32 个文件，ZIP 完整性、解压后哈希、中文文件名和 macOS 启动脚本权限通过。
- 本地截图和验收样例：`outputs/audit-2026-09-12/`（不上传仓库）。修改测试在单独的 127.0.0.1 浏览器存储进行，没有清空原 localhost 内容。

## 仍需明确的边界

- 完整依赖审计仍有 2 条 high，来自同一 `image-size 2.0.2 → vinext` 依赖链；不是生产依赖审计的残留。当前未使用图片上传解析，未证实能通过工作台界面利用。无同系列安全补丁，本轮未冒然迁移到 Vinext 大版本 beta。
- 现有原 localhost 存储无法读取，已保留并展示恢复提示；本次没有擅自清除或改写这份内容。
- 浏览器模拟不等于 Windows/macOS 与真实手机设备全部人工验收；系统关闭页面后的通知不在本地页面提醒能力范围内。
- 手机仍是手工导入的只读摘要，不是实时同步。更换浏览器、域名或端口不会自动迁移本机数据。
- 资料板块仍为现有资料目录，不在本轮扩建文件管理功能。

后续只推进现有能力的真实设备验收、客户首装演练和残留图片解析依赖的稳定补丁升级；不新增板块。
