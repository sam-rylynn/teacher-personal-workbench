# 教师个人工作台 · 可选首装服务与部署说明

> 产品口径：`v0.5.0` 候选版。代码采用 [MIT License](LICENSE)，可自行安装、商用、修改和再分发。本文说明可选的“一次安装 + 初始化配置”有偿服务，不是取得代码使用权的条件；具体服务费用以双方确认的订单或报价记录为准。

## 交付结果

完成首次交付后，老师应获得一份由自己控制的源码副本，并能在约定的电脑上启动工作台。可以直接使用公开仓库，或按老师需要建立个人仓库；私有仓库不是开源授权的要求。学生、成绩、课表和事项由老师在电脑浏览器本地维护；开发者不提供持续数据托管。

手机端采用专用文件手动导入，不是电脑数据的实时镜像。工作台也不包含内置 AI；老师自己的 Agent 只作为工作台之外的部署、排障、代码或文件辅助工具。

## 标准交付包含什么

1. 标准工作台源码及本版本配套文档，保留版权声明和 `LICENSE`；
2. 获取公开仓库源码；如约定建立个人仓库，则协助开通、上传源码和移交访问权限；
3. 一次初始化配置：
   - 工作台名称、老师称呼、学段与任教学科；
   - 班级字段、课表粘贴格式映射和默认提醒；
   - 品牌色与头像标记；
   - 演示数据清除确认；
4. 在老师本人的一台目标电脑上完成首次安装与启动协助；
5. 电脑正式数据、完整备份、手机查看文件和手机速记的使用教程；
6. 演示一次“电脑生成手机查看文件—老师传到手机—手机手动导入”的流程；
7. 协助老师使用自己的外部 Agent 拉取源码、执行部署命令或整理导入文件。

Windows 与 macOS 启动脚本都随源码提供，但只有在老师实际交付设备上完成 Runbook 并逐项勾选后，才能把相应系统记为验收通过。

## 标准交付不包含什么

- 后续持续维护、版本升级、故障响应或设备迁移；
- 核心逻辑修改或大规模功能定制；
- 云数据库、账号系统、跨设备同步或手机自动更新；
- 手机页面的云托管账号、域名、服务器及其持续费用；
- 内置 AI、模型账号、API 密钥接入或 AI 账号代管；
- 让 Agent 自动读取本地工作台，或让 AI 结果自动写入正式数据；
- 长期内容运营、数据代管或真实学生/家长资料的代导入。

后续微调、数据更新和内容迭代由老师自行完成，也可以自行选择外部 Agent 协助。老师主动交给外部 Agent 或 AI 的资料不再属于工作台的纯本地处理范围。

## 部署与移交流程

1. **确认设备与入口**：记录老师使用的 Windows 或 macOS 电脑，以及手机能否访问约定的查看页面。电脑 `localhost` 地址不能直接供另一台手机使用。
2. **获取源码**：从公开仓库获取约定提交，或按需建立老师自己的仓库并移交权限。保留版权声明和 `LICENSE`；源码仓库中不得放入真实学生资料、备份文件、手机查看文件或密钥。
3. **首次安装**：安装 Node.js `>=22.13.0`，在项目目录执行 `npm ci`。
4. **初始化**：完成字段、提醒、粘贴格式和外观配置，确认演示数据清除范围。
5. **电脑验收**：在老师的目标设备启动，验证保存、刷新、导入、备份和恢复。
6. **手机流程验收**：电脑生成专用文件，由老师传到手机并手动导入；确认正式学生、成绩、课表和事项在手机保持只读。
7. **速记验收**：在手机保存一条独立速记，复制内容并在电脑导入为待办事项。
8. **边界确认**：说明自动备份只在当前电脑浏览器内，手机文件不是完整备份，外部 Agent 不是内置 AI。
9. **签收移交**：双方按验收单记录实际结果；未实测项目不得预先勾选“通过”。

## 交付清单

- [ ] 老师本人控制的源码副本；如另建个人仓库，访问权限已移交
- [ ] 与候选提交一致的源码、`package-lock.json` 和启动脚本
- [ ] 完整的 `LICENSE` 与版权声明，已说明代码的 MIT 授权
- [ ] 如购买首装服务，已单独确认服务方、费用、范围、目标设备与验收方式
- [ ] 一次初始化配置记录
- [ ] 电脑、手机文件、速记和备份使用教程
- [ ] 目标电脑的安装与启动记录
- [ ] 实际手机页面入口及文件导入记录
- [ ] 交付验收单

## 交付验收单

| 验收项 | 通过标准 | 结果 |
|---|---|---|
| 候选版本 | `package.json` 为 `0.5.0`，记录实际交付提交 | ☐ 通过 ☐ 不通过 |
| 依赖安装 | 目标电脑执行 `npm ci` 成功 | ☐ 通过 ☐ 不通过 |
| 启动 | 使用对应系统脚本或 `npm run launch` 启动，实际地址可访问 | ☐ 通过 ☐ 不通过 |
| 桌面端 | 今日、学生、课表、事项、资料均可打开，修改后刷新仍保留 | ☐ 通过 ☐ 不通过 |
| 成绩导入 | 表格文本导入为待确认记录，可编辑、确认和删除 | ☐ 通过 ☐ 不通过 |
| 课表纠错 | 错误单次课可删除，每周固定课程可连同临时调整删除，单次调课或取消可撤销 | ☐ 通过 ☐ 不通过 |
| 数据恢复 | 完整 JSON 可导出，恢复前有内容确认 | ☐ 通过 ☐ 不通过 |
| 自动备份 | 本浏览器内可看到上一版本，并说明最多 3 份及同浏览器边界 | ☐ 通过 ☐ 不通过 |
| 手机入口 | 在一台真实手机上打开交付时记录的查看页 | ☐ 通过 ☐ 不通过 |
| 手机文件 | 电脑生成文件，传到真实手机并手动导入成功 | ☐ 通过 ☐ 不通过 |
| 手机只读 | 手机不能修改学生、成绩、课表和电脑正式事项 | ☐ 通过 ☐ 不通过 |
| 手机速记 | 速记仅存在手机端，复制后可由电脑导入为待办 | ☐ 通过 ☐ 不通过 |
| Agent 边界 | 已说明 Agent 仅为外部辅助，产品没有内置 AI 或密钥接入 | ☐ 通过 ☐ 不通过 |
| 演示数据 | 老师确认后清除虚构数据，再自行选择真实资料 | ☐ 通过 ☐ 不通过 |
| 开源授权 | 已保留 `LICENSE` 和版权声明，明确代码可自行使用、商用、修改和再分发 | ☐ 通过 ☐ 不通过 |
| 首装服务 | 如购买服务，双方已单独确认并留存费用、服务范围和验收记录 | ☐ 通过 ☐ 不通过 ☐ 不适用 |

## 数据与备份边界

- 电脑正式数据和自动备份位于当前电脑、当前浏览器、当前站点存储中；换浏览器、改端口或域名不会自动迁移，清理原站点数据会删除其中保存的资料。交付时应记录固定访问地址；迁移前先导出完整备份。
- 自动备份用于短期回退，最多保留 3 份。它不能替代老师另存的完整 JSON 文件。
- 手机查看文件只含摘要，不可用于恢复完整电脑工作区。
- 手机速记位于手机浏览器，与电脑数据、电脑自动备份和手机查看摘要分别保存。
- 工作台本身不把数据上传给开发者；老师主动把文件交给外部 Agent、AI、网盘或通信工具时，应自行确认接收方和资料范围。

## 候选版验证声明

`v0.5.0` 是当前候选口径，不表示已经打正式标签、上线或完成目标设备验收。仓库即使存在 CI 工作流，也不能据此声称 CI、Windows 或 macOS 已通过；应以实际交付提交对应的运行记录和实机验收单为准。

## 授权边界摘要

代码按根目录 [LICENSE](LICENSE) 中的 MIT License 授权：允许商用、修改、再分发及有偿部署，须保留版权声明和许可声明。可选首装服务另行约定，不限制 MIT 已授予的代码权利，也不自动包含持续维护。真实学生与家长资料、备份及密钥不得进入公共仓库。中文说明见《授权协议.md》。
